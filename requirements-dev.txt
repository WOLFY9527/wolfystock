-r requirements.txt

# Development owner: repository lint commands and CI gates
flake8>=7.1.0               # Python lint CLI used by scripts/ci_gate.sh

# Test owner: backend test runtime and repository pytest plugins
pytest>=8.0.0               # Test runner used by canonical and focused validation
